trigger:
- main

pool:
  name: SelfHostedPool

steps:
- script: |
    echo "SAFE FAILURE DRILL: intentional application pipeline failure."
    echo "Branch: drill/pipeline-failure"
    echo "This is a temporary pre-deployment validation failure."
    exit 1
  displayName: "SAFE FAILURE DRILL - Intentional Failure"
